# Changelog

## 0.1.0 — initial public scaffold
- 21-rule vulnerability catalogue (`rules/catalogue.yaml`) mapped to OWASP/ASVS.
- `governance.yaml` risk-manifest schema (proportionality engine) + worked example.
- Secure-by-default Next.js + Supabase templates (`templates/`).
- AI-context rule pack (`ai-context/CLAUDE.md`, `.cursorrules`).
- CI enforcement gates: gitleaks, Semgrep ruleset, common-secret blocklist, privacy-lint, PR checklist, pre-commit.
- Evaluation harness (`evaluation/`) with a demo corpus and reduction-rate metrics.
